# basic-WP-register-new-users-plugin
A basic WP plugin for registering new users. This assumes you have installed WordPress using "generic" configurations &amp; that you do not use a multi-site blog. This uses the wp_set_password() functions to set a new user password in the database. 
